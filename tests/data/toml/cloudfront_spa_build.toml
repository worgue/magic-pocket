[general]
region = "ap-southeast-1"
project_name = "testprj"
stages = ["dev"]

[s3]

[cloudfront.web]
routes = [
    { is_default = true, is_spa = true, build = { dir = "frontend/dist", cmd = "just frontend-build" }, origin_path = "/web/app" },
    { path_pattern = "/static/*", versioning = "content_hash", origin_path = "/web" },
]
