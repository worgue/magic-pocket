[project]
name = "pocket_example"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "magic-pocket[django]==0.15.0",
    "psycopg[binary]>=3.1",
    "boto3>=1.34.28",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.2.1",
]
