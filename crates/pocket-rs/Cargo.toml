[package]
name = "magic-pocket-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
toml = "1.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
aws-config = { version = "1", features = ["behavior-version-latest"] }
# aws-sdk-* は default の `rustls` feature が legacy TLS スタック (rustls 0.21 系) を
# 依存ツリーに残すため外す。HTTP client は default-https-client 側を使う
aws-sdk-secretsmanager = { version = "1", default-features = false, features = ["default-https-client", "rt-tokio"] }
aws-sdk-ssm = { version = "1", default-features = false, features = ["default-https-client", "rt-tokio"] }
aws-sdk-cloudformation = { version = "1", default-features = false, features = ["default-https-client", "rt-tokio"] }
aws-sdk-dsql = { version = "1", default-features = false, features = ["default-https-client", "rt-tokio"] }
aws-sdk-sqs = { version = "1", default-features = false, features = ["default-https-client", "rt-tokio"] }
thiserror = "2"
tracing = "0.1"
# RDS パスワードの percent-encode 用。aws-sdk-* が既に依存しているため
# 依存ツリーは増えない (Python 側 urllib.parse.quote との一致が要件)
percent-encoding = "2"
aws_lambda_events = { version = "1.2.0", default-features = false, features = ["sqs"] }
# origin verify middleware (feature "axum") 用。axum は HTTP serve 側の機能を
# 使わないため default features を外す。subtle は secret header の定数時間比較
# (Python 側 hmac.compare_digest との対応)
axum = { version = "0.8", optional = true, default-features = false }
subtle = { version = "2", optional = true }
# feature "inbound" (SES 受信 worker ヘルパ) 用。原本・受信情報の S3 読み書きと
# 原本 hash (Python 側 pocket.inbound と同じ sha256 hex)
aws-sdk-s3 = { version = "1", optional = true, default-features = false, features = ["default-https-client", "rt-tokio"] }
sha2 = { version = "0.11", optional = true }
hex = { version = "0.4", optional = true }

[features]
# pocket.django.origin_verify.OriginVerifyMiddleware 相当の axum middleware を有効化
axum = ["dep:axum", "dep:subtle"]
# pocket.inbound (SES 受信の原本・受信情報の保全 → 利用側処理) 相当の worker ヘルパ
inbound = ["dep:aws-sdk-s3", "dep:sha2", "dep:hex"]

[dev-dependencies]
# 本体は async fn を提供するだけで tokio API を直接使わない (runtime は利用側が
# 用意する)。テストの Runtime::new() と doc 例の #[tokio::main] にのみ必要
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
# origin verify middleware テストの Router::oneshot 用
tower = { version = "0.5", features = ["util"] }
